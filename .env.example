# Template for the OpenClaw secret set.
# Copy to config/.env on the deploy host (NOT committed, gitignored).
#   cp .env.example config/.env
#
# The real secret file lives at /opt/apps/openclaw/config/.env on .149
# and is injected via docker-compose env_file + copied by entrypoint.sh.

DEEPSEEK_API_KEY=
KAGI_API=
TELEGRAM_BOT_TOKEN=
OPENCLAW_GATEWAY_TOKEN=
GH_TOKEN=
# ── GitHub App „momo-bot" (replaces personal PAT GH_TOKEN — migration in progress) ──
# Installation tokens laufen nach ~1h ab; gh-app-auth.sh holt bei jedem
# Containerstart + bei Bedarf (401) ein frisches Token.
# Siehe SETUP.md für die App-Erstellung (Web-UI) und die ID-Ermittlung.
GH_APP_ID=
GH_APP_INSTALLATION_ID=
# Pfad zum Private Key (PEM) IM CONTAINER. Die Datei liegt auf dem Host unter
# ~/.secrets/momo-bot.pem (chmod 600) und wird via docker-compose gemountet
# (der Mount ist in docker-compose.yml auskommentiert — aktivieren, sobald
# die App existiert). Solange diese drei Vars leer sind, gilt der GH_TOKEN-Pfad.
GH_APP_PRIVATE_KEY_FILE=
GROQ_API_KEY=
DEEPGRAM_API_KEY=
GEMINI_API_KEY=
# Deploy webhook shared secret (matches GitHub repo secret DEPLOY_TOKEN)
DEPLOY_TOKEN=
# Infra access (Hetzner / Contabo / Cloudflare / INWX) — real values go into
# config/.env on .149 (gitignored, injected via docker-compose env_file + entrypoint.sh).
# Hetzner Cloud API — ein Token pro Projekt (die API ist projektbezogen, ein Token
# sieht nur sein eigenes Projekt). Server laufen im Projekt mittelerde. Die
# StorageBoxes sind ins Cloud-Projekt 11031986 migriert und laufen ueber die Cloud
# API (https://api.hetzner.com/v1/storage_boxes, Bearer-Token) - kein Robot-Zugang
# mehr noetig.
HETZNER_API_TOKEN_MITTELERDE=
HETZNER_API_TOKEN_STORAGEBOXES=
# Contabo Cloud API v2, OAuth2 password grant
CONTABO_CLIENT_ID=
CONTABO_CLIENT_SECRET=
# Contabo API User = CCP-Email; API Password = separates Passwort aus
# my.contabo.com/api/details (NICHT das CCP-Login-Passwort).
CONTABO_API_USER=
CONTABO_API_PASSWORD=
# Cloudflare API
CLOUDFLARE_API_TOKEN=
# INWX (Domain-Registrar, DomRobot) — Auth mit Benutzer + Passwort (kein API-Token)
INWX_API_USER=
INWX_API_PASSWORD=
